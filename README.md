# DataGrid-Excel
DataGrid Excel is a flexible Excel-style grid for Mendix. It renders dynamic rows and columns from Mendix data sources, with inline cell editing, sorting, row and column filters, column visibility, paging,   configurable cell rendering, click actions, and CSV export.
